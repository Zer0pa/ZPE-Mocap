# AGENTS.md instructions for /Users/Zer0pa/ZPE/ZPE Mocap

<INSTRUCTIONS>
NEVER: optimize for a narratable win instead of the governing objective; let local improvements substitute for the authority metric; close too early once U have something defensible-lookinG. NEVER REWARD HACK. NEVER RUSH.

ALWAYS: treat the top acceptance gate as sovereign; treat any regression on the authority metric as failure, regardless of secondary wins; keep docs and handover artifacts frozen until the real gate is met; stay in the fix loop instead of converting mixed evidence into a pass narrative. AVOID TOY/DEMO/REGRESSING FROM MAXIMAL OBJECTIVE PURSUIT.

CODING COMMANDMENTS
Thou shalt avoid deep nesting
Thou shalt avoid code duplication
Thou shalt not use naming that only you understand
Thou shalt use dependency injection to decouple components where it is useful.
Thou shalt decouple components by making use of interfaces where it makes sense to do so. Thou shalt separate functions into individual responsibilities where it makes sense.
</INSTRUCTIONS>
