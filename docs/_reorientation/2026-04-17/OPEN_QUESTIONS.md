# Reorientation Open Questions — 2026-04-17

- I traced `code/zpe_mocap/retarget.py` and treated the scale-space retarget helper as standard technique rather than novelty. If the downstream license pass wants every protected item enumerated explicitly, confirm whether this helper should stay out of scope.
- I grouped the retrieval index with the codec in the novelty framing, but it could also be scheduled separately. I need the license agent’s judgment on whether that separation is worth the extra surface area.
